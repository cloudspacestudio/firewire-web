export interface DeviceResolutionStrategy {
    resolutionId: string
    name: string
    formula: string
    projectId: string
    batchId: string
    ordinal: number
    createat: Date
    createby: string
    updateat: Date
    updateby: string
}