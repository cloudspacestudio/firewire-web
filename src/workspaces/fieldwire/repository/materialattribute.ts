export interface MaterialAttribute {
    materialAttributeId: string
    name: string
    statusId: string
    materialId: string
    projectId: string
    valueType: string
    defaultValue: string
    ordinal: number
}